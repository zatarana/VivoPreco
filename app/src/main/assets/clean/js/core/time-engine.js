window.TimeEngine=(function(){
  const n=v=>Number(v||0);
  const r=v=>Math.round(n(v));
  function ensure(data){data.timeLogs=Array.isArray(data.timeLogs)?data.timeLogs:[];data.activeTimer=data.activeTimer||null;return data;}
  function task(data,taskId){return (data.tasks||[]).find(t=>t.id===taskId);}
  function project(data,projectId){return (data.projects||[]).find(p=>p.id===projectId);}
  function logById(data,logId){ensure(data);return data.timeLogs.find(l=>l.id===logId);}
  function seriesIdFor(t){if(!t)return null;if(t.seriesId)return t.seriesId;if(t.recurrence&&t.recurrence!=='nenhuma')return 'series_'+String(t.title||t.id).toLowerCase().replace(/[^a-z0-9]+/g,'_');return null;}
  function addManualLog(data,taskId,minutes,note,date){ensure(data);const t=task(data,taskId);if(!t)throw new Error('Tarefa não encontrada.');const duration=Validators.positiveNumber(minutes,'Tempo');const log=Models.timeLog({taskId:t.id,projectId:t.projectId,seriesId:seriesIdFor(t),date:date||Models.today(),durationMinutes:r(duration),mode:'manual',note:Validators.safeText(note||'',240)});data.timeLogs.push(log);return log;}
  function updateLog(data,logId,input){ensure(data);const log=logById(data,logId);if(!log)throw new Error('Sessão de tempo não encontrada.');if(input.taskId!==undefined){const t=task(data,input.taskId);if(!t)throw new Error('Tarefa não encontrada.');log.taskId=t.id;log.projectId=t.projectId;log.seriesId=seriesIdFor(t);}if(input.durationMinutes!==undefined)log.durationMinutes=r(Validators.positiveNumber(input.durationMinutes,'Tempo'));if(input.date!==undefined)log.date=input.date||log.date;if(input.note!==undefined)log.note=Validators.safeText(input.note||'',240);if(input.mode!==undefined)log.mode=Validators.safeText(input.mode||log.mode,40);return log;}
  function deleteLog(data,logId){ensure(data);const idx=data.timeLogs.findIndex(l=>l.id===logId);if(idx<0)throw new Error('Sessão de tempo não encontrada.');const removed=data.timeLogs[idx];data.timeLogs.splice(idx,1);return removed;}
  function startTimer(data,taskId){ensure(data);const t=task(data,taskId);if(!t)throw new Error('Tarefa não encontrada.');if(data.activeTimer&&data.activeTimer.status==='running')throw new Error('Já existe um cronômetro em andamento.');data.activeTimer={taskId:t.id,projectId:t.projectId,seriesId:seriesIdFor(t),startedAt:Models.now(),accumulatedSeconds:n(data.activeTimer&&data.activeTimer.accumulatedSeconds),status:'running'};return data.activeTimer;}
  function pauseTimer(data){ensure(data);const timer=data.activeTimer;if(!timer||timer.status!=='running')throw new Error('Nenhum cronômetro em andamento.');const elapsed=(Date.now()-new Date(timer.startedAt).getTime())/1000;timer.accumulatedSeconds=r(n(timer.accumulatedSeconds)+elapsed);timer.status='paused';timer.startedAt=null;return timer;}
  function resumeTimer(data){ensure(data);const timer=data.activeTimer;if(!timer||timer.status!=='paused')throw new Error('Nenhum cronômetro pausado.');timer.status='running';timer.startedAt=Models.now();return timer;}
  function currentSeconds(data){ensure(data);const timer=data.activeTimer;if(!timer)return 0;if(timer.status==='running')return r(n(timer.accumulatedSeconds)+(Date.now()-new Date(timer.startedAt).getTime())/1000);return r(timer.accumulatedSeconds);}
  function finishTimer(data,note){ensure(data);const timer=data.activeTimer;if(!timer)throw new Error('Nenhum cronômetro ativo.');const seconds=currentSeconds(data);const minutes=Math.max(1,Math.round(seconds/60));const t=task(data,timer.taskId);if(!t)throw new Error('Tarefa não encontrada.');const log=Models.timeLog({taskId:t.id,projectId:t.projectId,seriesId:timer.seriesId||seriesIdFor(t),date:Models.today(),durationMinutes:minutes,mode:'timer',note:Validators.safeText(note||'',240)});data.timeLogs.push(log);data.activeTimer=null;return log;}
  function cancelTimer(data){ensure(data);data.activeTimer=null;}
  function logsForTask(data,taskId){ensure(data);return data.timeLogs.filter(l=>l.taskId===taskId);}
  function taskTotal(data,taskId){return r(logsForTask(data,taskId).reduce((s,l)=>s+n(l.durationMinutes),0));}
  function seriesTotal(data,seriesId){ensure(data);if(!seriesId)return 0;return r(data.timeLogs.filter(l=>l.seriesId===seriesId).reduce((s,l)=>s+n(l.durationMinutes),0));}
  function projectTotal(data,projectId){ensure(data);return r(data.timeLogs.filter(l=>l.projectId===projectId).reduce((s,l)=>s+n(l.durationMinutes),0));}
  function allTotal(data){ensure(data);return r(data.timeLogs.reduce((s,l)=>s+n(l.durationMinutes),0));}
  function dateTotal(data,date){ensure(data);return r(data.timeLogs.filter(l=>l.date===date).reduce((s,l)=>s+n(l.durationMinutes),0));}
  function monthTotal(data,month){ensure(data);return r(data.timeLogs.filter(l=>String(l.date||'').slice(0,7)===month).reduce((s,l)=>s+n(l.durationMinutes),0));}
  function projectReport(data,projectId,range){ensure(data);const p=project(data,projectId);const logs=data.timeLogs.filter(l=>l.projectId===projectId);const byTask={};logs.forEach(l=>{const t=task(data,l.taskId);const name=t?t.title:'Tarefa removida';byTask[name]=(byTask[name]||0)+n(l.durationMinutes);});const week=weekTotalForProject(data,projectId);const weeklyTarget=n(p&&p.weeklyTargetMinutes);const period=projectPeriodReport(data,projectId,range||'7');return {project:p,total:projectTotal(data,projectId),today:dateTotalForProject(data,projectId,Models.today()),week,month:monthTotalForProject(data,projectId,new Date().toISOString().slice(0,7)),weeklyTarget,weeklyProgress:weeklyTarget?Math.min(100,Math.round((week/weeklyTarget)*100)):0,streak:studyStreak(data,projectId),last7:lastDaysForProject(data,projectId,7),period,byTask};}
  function dateTotalForProject(data,projectId,date){ensure(data);return r(data.timeLogs.filter(l=>l.projectId===projectId&&l.date===date).reduce((s,l)=>s+n(l.durationMinutes),0));}
  function monthTotalForProject(data,projectId,month){ensure(data);return r(data.timeLogs.filter(l=>l.projectId===projectId&&String(l.date||'').slice(0,7)===month).reduce((s,l)=>s+n(l.durationMinutes),0));}
  function weekStart(date){const d=new Date(date);const day=d.getDay();const diff=(day===0?-6:1-day);d.setDate(d.getDate()+diff);d.setHours(0,0,0,0);return d;}
  function weekTotalForProject(data,projectId,date){ensure(data);const start=weekStart(date||new Date());const end=new Date(start);end.setDate(start.getDate()+7);return r(data.timeLogs.filter(l=>{const d=new Date(l.date+'T00:00:00');return l.projectId===projectId&&d>=start&&d<end;}).reduce((s,l)=>s+n(l.durationMinutes),0));}
  function isoOffset(days){const d=new Date();d.setDate(d.getDate()+days);return d.toISOString().slice(0,10);}
  function lastDaysForProject(data,projectId,days){ensure(data);const result=[];for(let i=days-1;i>=0;i--){const date=isoOffset(-i);result.push({date,total:dateTotalForProject(data,projectId,date)});}return result;}
  function yesterdayForProject(data,projectId){const date=isoOffset(-1);return [{date,total:dateTotalForProject(data,projectId,date)}];}
  function overallByTask(data,projectId){ensure(data);const map={};data.timeLogs.filter(l=>!projectId||l.projectId===projectId).forEach(l=>{const t=task(data,l.taskId);const key=t?t.title:'Tarefa removida';map[key]=(map[key]||0)+n(l.durationMinutes);});return map;}
  function projectPeriodReport(data,projectId,range){ensure(data);const key=String(range||'7');let points=[];let label='Últimos 7 dias';if(key==='yesterday'){points=yesterdayForProject(data,projectId);label='Dia anterior';}else if(key==='15'){points=lastDaysForProject(data,projectId,15);label='Últimos 15 dias';}else if(key==='30'){points=lastDaysForProject(data,projectId,30);label='Últimos 30 dias';}else if(key==='all'){points=[];label='Geral';}else{points=lastDaysForProject(data,projectId,7);}const total=key==='all'?projectTotal(data,projectId):r(points.reduce((s,p)=>s+n(p.total),0));return {range:key,label,points,total,byTask:overallByTask(data,projectId)};}
  function allPeriodReport(data,range){ensure(data);const key=String(range||'7');let points=[];let label='Últimos 7 dias';if(key==='yesterday'){const date=isoOffset(-1);points=[{date,total:dateTotal(data,date)}];label='Dia anterior';}else if(key==='15'){for(let i=14;i>=0;i--){const date=isoOffset(-i);points.push({date,total:dateTotal(data,date)});}label='Últimos 15 dias';}else if(key==='30'){for(let i=29;i>=0;i--){const date=isoOffset(-i);points.push({date,total:dateTotal(data,date)});}label='Últimos 30 dias';}else if(key==='all'){label='Geral';}else{for(let i=6;i>=0;i--){const date=isoOffset(-i);points.push({date,total:dateTotal(data,date)});}}const total=key==='all'?allTotal(data):r(points.reduce((s,p)=>s+n(p.total),0));return {range:key,label,points,total,byTask:overallByTask(data,null)};}
  function studyStreak(data,projectId){ensure(data);let streak=0;for(let i=0;i<365;i++){const date=isoOffset(-i);const total=projectId?dateTotalForProject(data,projectId,date):dateTotal(data,date);if(total>0)streak++;else if(i===0)continue;else break;}return streak;}
  function formatMinutes(minutes){const m=r(minutes);const h=Math.floor(m/60);const rest=m%60;if(h&&rest)return `${h}h${String(rest).padStart(2,'0')}`;if(h)return `${h}h`;return `${rest}min`;}
  return {ensure,logById,seriesIdFor,addManualLog,updateLog,deleteLog,startTimer,pauseTimer,resumeTimer,currentSeconds,finishTimer,cancelTimer,logsForTask,taskTotal,seriesTotal,projectTotal,allTotal,dateTotal,monthTotal,projectReport,dateTotalForProject,monthTotalForProject,weekTotalForProject,lastDaysForProject,yesterdayForProject,projectPeriodReport,allPeriodReport,studyStreak,formatMinutes};
})();
