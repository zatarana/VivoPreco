window.TasksUI=(function(){
  function render(data){
    Dom.setHeader('Tarefas','Projetos, próximas tarefas, filtros, subtarefas e comentários.');
    const tasks=TaskEngine.today(data);
    Dom.render(`<div class="grid">${Components.metric('Hoje',String(tasks.length),'primary')}${Components.metric('Atrasadas',String(TaskEngine.overdue(data).length),'danger')}${Components.metric('Abertas',String(TaskEngine.openTasks(data).length),'primary')}${Components.metric('Projetos',String(TaskEngine.projects(data).length),'warning')}</div><div class="row">${Components.button('+ Tarefa','newTask')}${Components.button('+ Projeto','newProject','secondary')}</div><div class="row">${Components.button('Próximas','showUpcoming','secondary')}${Components.button('Filtros','showTaskFilters','secondary')}</div><div class="row">${Components.button('Board','showTaskBoard','secondary')}${Components.button('Etiquetas','showLabels','secondary')}</div><h2 class="section">Hoje</h2>${taskList(data,tasks)}<h2 class="section">Projetos</h2>${projectList(data)}`);
  }
  function taskList(data,tasks){return tasks.length?`<div class="list">${tasks.map(t=>taskItem(data,t)).join('')}</div>`:Components.empty('Nenhuma tarefa.');}
  function taskItem(data,t){const sub=TaskEngine.subtasks(data,t.id);return `<div class="item task-accent ${t.done?'task-done':''}"><div class="row"><button class="task-check" data-action="${t.done?'reopenTask':'completeTask'}" data-id="${t.id}">${t.done?'✓':''}</button><div><h4>${Dom.esc(t.title)}</h4><div class="meta">${Dom.esc(projectName(data,t.projectId))} • ${Dom.date(t.dueDate)} • ${Dom.esc(t.priority)} ${t.labels.map(l=>`<span class="pill">#${Dom.esc(l)}</span>`).join('')} ${sub.length?`<span class="pill">${sub.filter(s=>s.done).length}/${sub.length} sub</span>`:''}</div>${t.description?`<p class="meta">${Dom.esc(t.description)}</p>`:''}</div></div><div class="actions">${Components.mini('Editar','editTask',t.id)}${Components.mini('Subtarefa','newSubtask',t.id)}${Components.mini('Comentário','newComment',t.id)}${Components.mini('Excluir','deleteTask',t.id,'danger')}</div>${sub.length?`<div class="list" style="margin-top:8px">${sub.map(s=>taskItem(data,s)).join('')}</div>`:''}</div>`;}
  function projectList(data){return data.projects.map(p=>`<div class="item" style="border-left-color:${p.color||'var(--color-primary)'}"><h4>${Dom.esc(p.name)}</h4><div class="meta">${TaskEngine.byProject(data,p.id).length} tarefa(s) abertas • ${Dom.esc(p.view)}</div><div class="actions">${Components.mini('Abrir','openProject',p.id,'primary')}${Components.mini('Nova tarefa','newProjectTask',p.id)}</div></div>`).join('');}
  function projectName(data,id){return (data.projects.find(p=>p.id===id)||{}).name||'Projeto';}
  function projectIdFromName(data,name){return (data.projects.find(p=>p.name===name)||data.projects[0]).id;}
  function newTaskForm(projectId){const data=StorageService.read();const p=projectId?(data.projects.find(x=>x.id===projectId)||data.projects[0]):data.projects[0];Dom.openSheet(Components.sheet('Nova tarefa',Components.field('taskTitle','Título')+Components.field('taskDesc','Descrição')+Components.select('taskProject','Projeto',data.projects.map(p=>p.name),p.name)+Components.field('taskDue','Data','date',Models.today())+Components.select('taskPriority','Prioridade',['P1','P2','P3','P4'],'P4')+Components.field('taskLabels','Etiquetas'), 'saveTask'));}
  function saveTask(){StorageService.update(d=>{const project=d.projects.find(p=>p.name===Dom.$('#taskProject').value)||d.projects[0];TaskEngine.addTask(d,{title:Dom.$('#taskTitle').value||'Nova tarefa',description:Dom.$('#taskDesc').value||'',projectId:project.id,dueDate:Dom.$('#taskDue').value||null,priority:Dom.$('#taskPriority').value,labels:Dom.$('#taskLabels').value.split(',').map(x=>x.trim()).filter(Boolean)});});Dom.closeSheet();App.render();Dom.toast('Tarefa criada.');}
  function editTaskForm(id){const data=StorageService.read();const t=data.tasks.find(x=>x.id===id);if(!t)return;Dom.openSheet(Components.sheet('Editar tarefa',Components.field('editTaskTitle','Título','text',t.title)+Components.field('editTaskDesc','Descrição','text',t.description||'')+Components.select('editTaskProject','Projeto',data.projects.map(p=>p.name),projectName(data,t.projectId))+Components.field('editTaskDue','Data','date',t.dueDate||Models.today())+Components.select('editTaskPriority','Prioridade',['P1','P2','P3','P4'],t.priority)+Components.field('editTaskLabels','Etiquetas','text',(t.labels||[]).join(', ')),`saveEditTask:${id}`));}
  function saveEditTask(id){StorageService.update(d=>TaskEngine.updateTask(d,id,{title:Dom.$('#editTaskTitle').value,description:Dom.$('#editTaskDesc').value,projectId:projectIdFromName(d,Dom.$('#editTaskProject').value),dueDate:Dom.$('#editTaskDue').value||null,priority:Dom.$('#editTaskPriority').value,labels:Dom.$('#editTaskLabels').value.split(',').map(x=>x.trim()).filter(Boolean)}));Dom.closeSheet();App.render();Dom.toast('Tarefa atualizada.');}
  function newProjectForm(){Dom.openSheet(Components.sheet('Novo projeto',Components.field('projectName','Nome')+Components.select('projectView','Visualização',['list','board'],'list'), 'saveProject'));}
  function saveProject(){StorageService.update(d=>TaskEngine.addProject(d,{name:Dom.$('#projectName').value||'Projeto',view:Dom.$('#projectView').value,sections:['A Fazer','Em andamento','Concluído']}));Dom.closeSheet();App.render();Dom.toast('Projeto criado.');}
  function completeTask(id){StorageService.update(d=>TaskEngine.complete(d,id));App.render();Dom.toast('Tarefa concluída.');}
  function reopenTask(id){StorageService.update(d=>TaskEngine.reopen(d,id));App.render();Dom.toast('Tarefa reaberta.');}
  function deleteTask(id){if(!confirm('Excluir tarefa e subtarefas?'))return;StorageService.update(d=>TaskEngine.deleteTask(d,id));App.render();Dom.toast('Tarefa excluída.');}
  function newSubtaskForm(id){Dom.openSheet(Components.sheet('Nova subtarefa',Components.field('subTitle','Título')+Components.field('subDue','Data','date',Models.today())+Components.select('subPriority','Prioridade',['P1','P2','P3','P4'],'P4'),`saveSubtask:${id}`));}
  function saveSubtask(id){StorageService.update(d=>TaskEngine.addSubtask(d,id,{title:Dom.$('#subTitle').value||'Subtarefa',dueDate:Dom.$('#subDue').value||null,priority:Dom.$('#subPriority').value}));Dom.closeSheet();App.render();Dom.toast('Subtarefa criada.');}
  function newCommentForm(id){Dom.openSheet(Components.sheet('Novo comentário',`<label class="field"><span>Comentário</span><textarea id="commentText"></textarea></label>`,`saveComment:${id}`));}
  function saveComment(id){StorageService.update(d=>TaskEngine.addComment(d,id,Dom.$('#commentText').value));Dom.closeSheet();App.render();Dom.toast('Comentário salvo.');}
  function showUpcoming(data){Dom.setHeader('Próximas tarefas','Tarefas futuras organizadas por data.');Dom.render(`${Components.button('+ Nova tarefa','newTask')}${taskList(data,TaskEngine.upcoming(data))}`);}
  function showTaskFilters(data){Dom.setHeader('Filtros','Visões rápidas das tarefas.');Dom.render(`${Components.card('Atrasadas',taskList(data,TaskEngine.overdue(data)))}${Components.card('Prioridade 1',taskList(data,TaskEngine.openTasks(data).filter(t=>t.priority==='P1')))}${Components.card('Sem data',taskList(data,TaskEngine.openTasks(data).filter(t=>!t.dueDate)))}`);}
  function showLabels(data){Dom.setHeader('Etiquetas','Tarefas agrupadas por etiqueta.');const labels=TaskEngine.labels(data);Dom.render(labels.length?labels.map(l=>`<div class="item"><h4>#${Dom.esc(l)}</h4><div class="meta">${TaskEngine.byLabel(data,l).length} tarefa(s)</div><div class="actions">${Components.mini('Abrir','openLabel',l,'primary')}</div></div>`).join(''):Components.empty('Nenhuma etiqueta.'));}
  function openLabel(label){const data=StorageService.read();Dom.setHeader('#'+label,'Tarefas com esta etiqueta.');Dom.render(taskList(data,TaskEngine.byLabel(data,label)));}
  function openProject(id){const data=StorageService.read();const p=data.projects.find(x=>x.id===id);Dom.setHeader(p?p.name:'Projeto','Tarefas do projeto.');Dom.render(`${Components.button('+ Nova tarefa','newProjectTask','secondary').replace('data-action="newProjectTask"','data-action="newProjectTask" data-id="'+id+'"')}${p&&p.view==='board'?board(data,id):taskList(data,TaskEngine.byProject(data,id))}`);}
  function board(data,projectId){const cols=['A Fazer','Em andamento','Concluída'];return `<div class="board">${cols.map(c=>`<div class="column"><h3>${c}</h3>${data.tasks.filter(t=>t.projectId===projectId&&(t.status===c||(c==='Concluída'&&t.done))).map(t=>`<div class="item"><h4>${Dom.esc(t.title)}</h4><div class="meta">${Dom.date(t.dueDate)} • ${Dom.esc(t.priority)}</div><div class="actions">${Components.mini('Editar','editTask',t.id)}</div></div>`).join('')||'<p class="meta">Sem tarefas.</p>'}</div>`).join('')}</div>`;}
  function showTaskBoard(data){Dom.setHeader('Board','Quadro geral por status.');Dom.render(board(data,data.projects[0]?.id||'project_inbox'));}
  return {render,newTaskForm,saveTask,editTaskForm,saveEditTask,newProjectForm,saveProject,completeTask,reopenTask,deleteTask,newSubtaskForm,saveSubtask,newCommentForm,saveComment,showUpcoming,showTaskFilters,showLabels,openLabel,openProject,showTaskBoard};
})();
