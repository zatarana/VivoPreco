window.FinanceUI=(function(){
  function render(data){
    Dom.setHeader('Finanças','Transações, contas e carteiras com fonte única de verdade.');
    const tx=data.transactions.slice().reverse().slice(0,8);
    Dom.render(`<section class="finance-hero"><small>Saldo das carteiras</small><h2>${Dom.money(FinanceEngine.totalWalletBalance(data))}</h2><div class="grid">${Components.metric('Receitas',Dom.money(FinanceEngine.income(data)),'success')}${Components.metric('Despesas',Dom.money(FinanceEngine.expense(data)),'danger')}${Components.metric('A receber',Dom.money(FinanceEngine.receivable(data)),'success')}${Components.metric('A pagar',Dom.money(FinanceEngine.payable(data)),'danger')}</div></section>${Components.button('+ Nova transação','newTransaction')}${Components.button('+ Nova conta','newBill','secondary')}<h2 class="section">Últimas transações</h2><div class="list">${tx.length?tx.map(t=>`<div class="item ${t.type==='receita'?'success-accent':'danger-accent'}"><h4>${Dom.esc(t.description)}</h4><div class="meta">${Dom.esc(t.category)} • ${Dom.date(t.date)} • ${Dom.money(t.value)}</div></div>`).join(''):Components.empty('Nenhuma transação.')}</div><h2 class="section">Contas pendentes</h2>${billList(data)}`);
  }
  function billList(data){const bills=data.bills.filter(b=>FinanceEngine.billRemaining(b)>0);return bills.length?bills.map(b=>`<div class="item"><h4>${Dom.esc(b.name)}</h4><div class="meta">${b.flow==='A_PAGAR'?'A pagar':'A receber'} • Pendente ${Dom.money(FinanceEngine.billRemaining(b))} • ${Dom.date(b.dueDate)}</div><div class="actions">${Components.mini('Liquidar','settleBill',b.id,'primary')}</div></div>`).join(''):Components.empty('Nenhuma conta pendente.');}
  function newTransactionForm(){Dom.openSheet(Components.sheet('Nova transação',Components.select('txType','Tipo',['despesa','receita'],'despesa')+Components.field('txValue','Valor','number')+Components.field('txDesc','Descrição')+Components.field('txCat','Categoria','text','Geral'), 'saveTransaction'));}
  function saveTransaction(data){StorageService.update(d=>FinanceEngine.addTransaction(d,{type:Dom.$('#txType').value,value:Number(Dom.$('#txValue').value||0),description:Dom.$('#txDesc').value||'Sem descrição',category:Dom.$('#txCat').value||'Geral'}));Dom.closeSheet();App.render();Dom.toast('Transação salva.');}
  function newBillForm(){Dom.openSheet(Components.sheet('Nova conta',Components.field('billName','Nome')+Components.select('billFlow','Tipo',['A_PAGAR','A_RECEBER'],'A_PAGAR')+Components.field('billValue','Valor previsto','number')+Components.field('billDue','Vencimento','date',Models.today()), 'saveBill'));}
  function saveBill(data){StorageService.update(d=>FinanceEngine.addBill(d,{name:Dom.$('#billName').value||'Conta',flow:Dom.$('#billFlow').value,expected:Number(Dom.$('#billValue').value||0),dueDate:Dom.$('#billDue').value||Models.today()}));Dom.closeSheet();App.render();Dom.toast('Conta criada.');}
  function settleBill(data,id){StorageService.update(d=>FinanceEngine.settleBill(d,id,FinanceEngine.billRemaining(d.bills.find(b=>b.id===id)),Models.today()));App.render();Dom.toast('Conta liquidada e transação gerada.');}
  return {render,newTransactionForm,saveTransaction,newBillForm,saveBill,settleBill};
})();
