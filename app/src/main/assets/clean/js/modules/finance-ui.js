window.FinanceUI=(function(){
  function moneyValue(id){return window.MoneyMask?MoneyMask.parse(Dom.$('#'+id).value):Validators.normalizeMoney(Dom.$('#'+id).value);}
  function render(data){
    Dom.setHeader('Finanças','Transações, contas, carteiras e transferências.');
    const tx=data.transactions.slice().reverse().slice(0,8);
    Dom.render(`<section class="finance-hero"><small>Saldo das carteiras</small><h2>${Dom.money(FinanceEngine.totalWalletBalance(data))}</h2><div class="grid">${Components.metric('Receitas',Dom.money(FinanceEngine.income(data)),'success')}${Components.metric('Despesas',Dom.money(FinanceEngine.expense(data)),'danger')}${Components.metric('A receber',Dom.money(FinanceEngine.receivable(data)),'success')}${Components.metric('A pagar',Dom.money(FinanceEngine.payable(data)),'danger')}</div></section><div class="row">${Components.button('+ Transação','newTransaction')}${Components.button('Transferir','newTransfer','secondary')}</div><div class="row">${Components.button('+ Conta','newBill','secondary')}${Components.button('Carteiras','showWallets','secondary')}</div><div class="row">${Components.button('Contas','showBills','secondary')}${Components.button('Relatório','showFinanceReport','secondary')}</div><h2 class="section">Últimas transações</h2><div class="list">${tx.length?tx.map(transactionItem).join(''):Components.empty('Nenhuma transação.')}</div><h2 class="section">Contas pendentes</h2>${billList(data)}`);
  }
  function transactionItem(t){
    if(t.type==='transferencia')return `<div class="item"><h4>${Dom.esc(t.description||'Transferência')}</h4><div class="meta">${walletName(App.data(),t.fromWalletId)} → ${walletName(App.data(),t.toWalletId)} • ${Dom.date(t.date)} • ${Dom.money(t.value)}</div></div>`;
    return `<div class="item ${t.type==='receita'?'success-accent':'danger-accent'}"><h4>${Dom.esc(t.description)}</h4><div class="meta">${Dom.esc(t.category)} • ${walletName(App.data(),t.walletId)} • ${Dom.date(t.date)} • ${Dom.money(t.value)}</div></div>`;
  }
  function billList(data){const bills=data.bills.filter(b=>FinanceEngine.billRemaining(b)>0).slice(0,5);return bills.length?bills.map(b=>`<div class="item"><h4>${Dom.esc(b.name)}</h4><div class="meta">${b.flow==='A_PAGAR'?'A pagar':'A receber'} • Pendente ${Dom.money(FinanceEngine.billRemaining(b))} • ${Dom.date(b.dueDate)} • ${walletName(data,b.walletId)}</div><div class="actions">${Components.mini('Liquidar','settleBillForm',b.id,'primary')}${Components.mini('Editar','editBill',b.id)}</div></div>`).join(''):Components.empty('Nenhuma conta pendente.');}
  function defaultWalletId(data){return (data.preferences&&data.preferences.defaultWalletId)||'wallet_main';}
  function defaultWallet(data){return data.wallets.find(w=>w.id===defaultWalletId(data))||data.wallets[0];}
  function walletName(data,id){return (data.wallets.find(w=>w.id===id)||{}).name||'Principal';}
  function walletOptions(data){return data.wallets.map(w=>w.name);}
  function walletIdFromName(data,name){return (data.wallets.find(w=>w.name===name)||defaultWallet(data)||data.wallets[0]).id;}
  function walletById(data,id){return data.wallets.find(w=>w.id===id);}
  function walletInUse(data,id){return data.transactions.some(t=>t.walletId===id||t.fromWalletId===id||t.toWalletId===id)||data.bills.some(b=>b.walletId===id);}
  function billById(data,id){return data.bills.find(b=>b.id===id);}
  function newTransactionForm(){const data=App.data();const dw=defaultWallet(data);Dom.openSheet(Components.sheet('Nova transação',Components.select('txType','Tipo',['despesa','receita'],'despesa')+Components.field('txValue','Valor','text')+Components.field('txDesc','Descrição')+Components.field('txCat','Categoria','text','Geral')+Components.select('txWallet','Carteira',walletOptions(data),dw.name), 'saveTransaction'));}
  function saveTransaction(data){StorageService.update(d=>FinanceEngine.addTransaction(d,{type:Dom.$('#txType').value,value:moneyValue('txValue'),description:Dom.$('#txDesc').value||'Sem descrição',category:Dom.$('#txCat').value||'Geral',walletId:walletIdFromName(d,Dom.$('#txWallet').value)}));Dom.closeSheet();App.render();Dom.toast('Transação salva.');}
  function newTransferForm(){const data=App.data();const wallets=walletOptions(data);Dom.openSheet(Components.sheet('Transferência entre carteiras',Components.field('transferValue','Valor','text')+Components.select('transferFrom','Origem',wallets,walletName(data,defaultWalletId(data)))+Components.select('transferTo','Destino',wallets,wallets[1]||wallets[0])+Components.field('transferDesc','Descrição','text','Transferência'), 'saveTransfer'));}
  function saveTransfer(){StorageService.update(d=>FinanceEngine.addTransfer(d,{value:moneyValue('transferValue'),fromWalletId:walletIdFromName(d,Dom.$('#transferFrom').value),toWalletId:walletIdFromName(d,Dom.$('#transferTo').value),description:Dom.$('#transferDesc').value||'Transferência'}));Dom.closeSheet();App.render();Dom.toast('Transferência salva.');}
  function newBillForm(){const data=App.data();const dw=defaultWallet(data);Dom.openSheet(Components.sheet('Nova conta',Components.field('billName','Nome')+Components.select('billFlow','Tipo',['A_PAGAR','A_RECEBER'],'A_PAGAR')+Components.field('billValue','Valor previsto','text')+Components.field('billDue','Vencimento','date',Models.today())+Components.select('billWallet','Carteira padrão para liquidação',walletOptions(data),dw.name), 'saveBill'));}
  function saveBill(data){StorageService.update(d=>FinanceEngine.addBill(d,{name:Dom.$('#billName').value||'Conta',flow:Dom.$('#billFlow').value,expected:moneyValue('billValue'),dueDate:Dom.$('#billDue').value||Models.today(),walletId:walletIdFromName(d,Dom.$('#billWallet').value)}));Dom.closeSheet();App.render();Dom.toast('Conta criada.');}
  function settleBillForm(id){const data=App.data();const b=billById(data,id);if(!b)return;Dom.openSheet(Components.sheet('Liquidar conta',Components.field('settleValue','Valor pago/recebido','text',Dom.money(FinanceEngine.billRemaining(b)))+Components.field('settleDate','Data','date',Models.today()),`saveSettleBill:${id}`));}
  function saveSettleBill(id){StorageService.update(d=>FinanceEngine.settleBill(d,id,moneyValue('settleValue'),Dom.$('#settleDate').value||Models.today()));Dom.closeSheet();App.render();Dom.toast('Conta liquidada.');}
  function settleBill(data,id){StorageService.update(d=>FinanceEngine.settleBill(d,id,FinanceEngine.billRemaining(d.bills.find(b=>b.id===id)),Models.today()));App.render();Dom.toast('Conta liquidada e transação gerada.');}
  function showWallets(data){
    Dom.setHeader('Carteiras','Saldos podem ficar positivos ou negativos. A carteira padrão já vem selecionada nas transações.');
    Dom.render(`${Components.button('+ Nova carteira','newWallet')}<div class="grid">${Components.metric('Saldo total',Dom.money(FinanceEngine.totalWalletBalance(data)),FinanceEngine.totalWalletBalance(data)>=0?'success':'danger')}${Components.metric('Carteira padrão',walletName(data,defaultWalletId(data)),'primary')}</div>${data.wallets.map(w=>`<div class="item ${FinanceEngine.walletBalance(data,w.id)>=0?'success-accent':'danger-accent'}"><h4>${Dom.esc(w.name)} ${w.id===defaultWalletId(data)?'<span class="pill">Padrão</span>':''}</h4><div class="meta">${Dom.esc(w.type)} • Inicial ${Dom.money(w.initialBalance)} • Atual ${Dom.money(FinanceEngine.walletBalance(data,w.id))} • Total: ${w.includeInTotal?'Sim':'Não'}</div><div class="actions">${w.id!==defaultWalletId(data)?Components.mini('Definir padrão','setDefaultWallet',w.id,'primary'):''}${Components.mini('Editar','editWallet',w.id)}${data.wallets.length>1&&!walletInUse(data,w.id)?Components.mini('Excluir','deleteWallet',w.id,'danger'):''}</div></div>`).join('')}`);
  }
  function newWalletForm(){Dom.openSheet(Components.sheet('Nova carteira',Components.field('walletName','Nome')+Components.field('walletType','Tipo','text','Conta Corrente')+Components.field('walletInitial','Saldo inicial pode ser negativo','text','0')+Components.select('walletInclude','Incluir no total',['Sim','Não'],'Sim')+Components.select('walletDefault','Definir como padrão',['Não','Sim'],'Não'), 'saveWallet'));}
  function saveWallet(){StorageService.update(d=>{const wallet=Models.wallet({id:Models.uid('wallet'),name:Dom.$('#walletName').value||'Carteira',type:Dom.$('#walletType').value||'Conta',initialBalance:moneyValue('walletInitial'),includeInTotal:Dom.$('#walletInclude').value==='Sim'});d.wallets.push(wallet);d.preferences=d.preferences||{};if(Dom.$('#walletDefault').value==='Sim')d.preferences.defaultWalletId=wallet.id;});Dom.closeSheet();App.render();Dom.toast('Carteira criada.');}
  function editWalletForm(id){const data=App.data();const w=walletById(data,id);if(!w)return;Dom.openSheet(Components.sheet('Editar carteira',Components.field('editWalletName','Nome','text',w.name)+Components.field('editWalletType','Tipo','text',w.type)+Components.field('editWalletInitial','Saldo inicial','text',Dom.money(w.initialBalance))+Components.select('editWalletInclude','Incluir no total',['Sim','Não'],w.includeInTotal?'Sim':'Não'),`saveEditWallet:${id}`));}
  function saveEditWallet(id){StorageService.update(d=>{const w=walletById(d,id);if(!w)throw new Error('Carteira não encontrada.');w.name=Dom.$('#editWalletName').value||w.name;w.type=Dom.$('#editWalletType').value||w.type;w.initialBalance=moneyValue('editWalletInitial');w.includeInTotal=Dom.$('#editWalletInclude').value==='Sim';});Dom.closeSheet();showWallets(App.data());Dom.toast('Carteira atualizada.');}
  function deleteWallet(id){if(!confirm('Excluir carteira?'))return;StorageService.update(d=>{if(d.wallets.length<=1)throw new Error('É necessário manter ao menos uma carteira.');if(walletInUse(d,id))throw new Error('Não é seguro excluir carteira usada por transações ou contas.');d.wallets=d.wallets.filter(w=>w.id!==id);if(d.preferences&&d.preferences.defaultWalletId===id)d.preferences.defaultWalletId=d.wallets[0].id;});showWallets(App.data());Dom.toast('Carteira excluída.');}
  function setDefaultWallet(id){StorageService.update(d=>{d.preferences=d.preferences||{};if(d.wallets.find(w=>w.id===id))d.preferences.defaultWalletId=id;});App.render();Dom.toast('Carteira padrão atualizada.');}
  function showBills(data){
    Dom.setHeader('Contas','Pendências, liquidações, edição e exclusão segura.');
    const sorted=data.bills.slice().sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate)));
    Dom.render(`${Components.button('+ Nova conta','newBill')}<div class="grid">${Components.metric('A pagar',Dom.money(FinanceEngine.payable(data)),'danger')}${Components.metric('A receber',Dom.money(FinanceEngine.receivable(data)),'success')}</div>${sorted.length?sorted.map(b=>`<div class="item ${FinanceEngine.billRemaining(b)>0?'':'success-accent'}"><h4>${Dom.esc(b.name)}</h4><div class="meta">${b.flow==='A_PAGAR'?'A pagar':'A receber'} • Previsto ${Dom.money(b.expected)} • Liquidado ${Dom.money(FinanceEngine.billSettled(b))} • Pendente ${Dom.money(FinanceEngine.billRemaining(b))} • ${Dom.esc(b.status)} • ${walletName(data,b.walletId)}</div><div class="actions">${FinanceEngine.billRemaining(b)>0?Components.mini('Liquidar','settleBillForm',b.id,'primary'):''}${Components.mini('Editar','editBill',b.id)}${FinanceEngine.billSettled(b)<=0?Components.mini('Excluir','deleteBill',b.id,'danger'):''}</div></div>`).join(''):Components.empty('Nenhuma conta.')}`);
  }
  function editBillForm(id){const data=App.data();const b=billById(data,id);if(!b)return;Dom.openSheet(Components.sheet('Editar conta',Components.field('editBillName','Nome','text',b.name)+Components.select('editBillFlow','Tipo',['A_PAGAR','A_RECEBER'],b.flow)+Components.field('editBillValue','Valor previsto','text',Dom.money(b.expected))+Components.field('editBillDue','Vencimento','date',b.dueDate)+Components.select('editBillWallet','Carteira',walletOptions(data),walletName(data,b.walletId)),`saveEditBill:${id}`));}
  function saveEditBill(id){StorageService.update(d=>FinanceEngine.updateBill(d,id,{name:Dom.$('#editBillName').value,flow:Dom.$('#editBillFlow').value,expected:moneyValue('editBillValue'),dueDate:Dom.$('#editBillDue').value,walletId:walletIdFromName(d,Dom.$('#editBillWallet').value)}));Dom.closeSheet();showBills(App.data());Dom.toast('Conta atualizada.');}
  function deleteBill(id){if(!confirm('Excluir esta conta?'))return;StorageService.update(d=>FinanceEngine.deleteBill(d,id));showBills(App.data());Dom.toast('Conta excluída.');}
  function showFinanceReport(data){
    Dom.setHeader('Relatório financeiro','Resumo calculado a partir de transações reais.');
    const cats={};data.transactions.filter(t=>t.type==='despesa').forEach(t=>cats[t.category]=(cats[t.category]||0)+Number(t.value||0));
    Dom.render(`<div class="grid">${Components.metric('Receitas',Dom.money(FinanceEngine.income(data)),'success')}${Components.metric('Despesas',Dom.money(FinanceEngine.expense(data)),'danger')}${Components.metric('Saldo',Dom.money(FinanceEngine.totalWalletBalance(data)),FinanceEngine.totalWalletBalance(data)>=0?'success':'danger')}${Components.metric('Projetado',Dom.money(FinanceEngine.projectedBalance(data,DebtEngine.minPayment(data))),'primary')}</div>${Components.card('Despesas por categoria',Object.keys(cats).map(k=>`${Dom.esc(k)}: <b>${Dom.money(cats[k])}</b>`).join('<br>')||'Sem despesas registradas.')}`);
  }
  return {render,newTransactionForm,saveTransaction,newTransferForm,saveTransfer,newBillForm,saveBill,settleBill,settleBillForm,saveSettleBill,showWallets,newWalletForm,saveWallet,editWalletForm,saveEditWallet,deleteWallet,setDefaultWallet,showBills,editBillForm,saveEditBill,deleteBill,showFinanceReport};
})();
